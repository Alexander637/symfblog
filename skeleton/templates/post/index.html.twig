{% extends 'base.html.twig' %}

{% block title %}Posts for Guests{% endblock %}

{% block body %}

    <h1>Give your feedback!</h1>

    {% for post in posts %}
        <h3>{{ post.title }}</h3>
        <h4>{{ post.text }}</h4>
    {% endfor %}

{% endblock %}


